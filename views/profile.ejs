<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>Document</title>
</head>

<body>
    <div class="w-full min-h-screen bg-zinc-800 p-10">
        <div class="flex justify-end items-end">
            <a class="bg-red-500 text-white p-3 rounded-md" href="/logout">Logout</a>
        </div>
        <h1 class="text-5xl text-white mb-2">Hello <%= user.name %>👋</h1>
        <h5 class="text-white mb-10">You can create new posts</h5>
        <form action="/post" method="post" class="">
            <textarea name="content" class="block resize-none rounded-md bg-zinc-700 min-w-100 p-3 mb-2 text-white"
                placeholder="Write your thoughts.."></textarea>
            <input class="block bg-blue-500 text-white p-3 rounded-md" type="submit" value="Create new post">
        </form>

        <div>
            <h1 class="text-2xl text-white mt-15 mb-5">Your Posts</h1>
            <div class="post-container">
                <% user.posts.reverse().forEach(function(post) { %>
                    <div class="post max-w-70 bg-zinc-900 border-2 border-zinc-900 rounded-md p-3 m-5">
                        <span class="inline-block text-blue-500 mb-2"><%= user.username  %></span>
                        <p class="text-white"><%= post.content %></p>
                        <div class="flex gap-3 justify-end">
                            <a href="/edit/<%= post._id %>" class="text-zinc-500">Edit</a>
                            <a href="/delete/<%= post._id %>" class="text-red-500">Delete</a>
                        </div>
                    </div>
                    <%})%>
            </div>
        </div>
    </div>
</body>

</html>